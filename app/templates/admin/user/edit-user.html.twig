{% extends 'admin/default/default.html.twig' %}

{% block content %}
    <h1>Edit profile</h1>
    {{ form(form) }}
    {% if user.role == constant('App\\Entity\\User::ROLE_SCOUT') or user.role == constant('App\\Entity\\User::ROLE_WOLVES') %}
        <button id="promote" data-target="{{ user.id }}">Promote</button>
    {% endif %}
    {% include '/admin/reusable-parts/promote-modal.htwl.twig' %}
{% endblock %}