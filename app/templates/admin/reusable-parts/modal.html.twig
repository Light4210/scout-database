<div class="modal micromodal-slide" id="modal-1" aria-hidden="true">
    <div class="modal__overlay" tabindex="-1" data-micromodal-close>
        <div class="modal__container" role="dialog" aria-modal="true" aria-labelledby="modal-1-title">
            <header class="modal__header">
                <h2 class="modal__title" id="modal-1-title">
                    Select new sheaf for this struct
                </h2>
                <button class="modal__close" aria-label="Close modal" data-micromodal-close></button>
            </header>
            <form method="get" action="{{ path('struct.set.sheaf', {'struct_id': struct.id}) }}">
                <main class="modal__content" id="modal-1-content">
                    <select name="user">
                        {% for member in struct.getMembers %}
                            <option value="{{ member.getId }}">{{ member.getName }} {{ member.getSurname }}</option>
                        {% endfor %}
                    </select>
                </main>
                <footer class="modal__footer">
                    <button type="submit" class="modal__btn modal__btn-primary">Set</button>
                    <button class="modal__btn" data-micromodal-close aria-label="Close this dialog window">Close
                    </button>
                </footer>
            </form>
        </div>
    </div>
</div>